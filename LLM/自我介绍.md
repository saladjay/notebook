## 自我介绍

面试官好，我叫邓雍杰。我今年31岁，广东工业大学的毕业生，大学期间开始接触深度学习，目前有6到7年的人工智能经验。在医疗影像，智能家居和工业视觉三个行业工作过。在最近的一份工作里，我在公司内担任深度学习小组组长，主导过标注云平台的开发，深度学习模型提速，更新过平台深度学习框架，异常检测模型开发和优化，目标检测与分割算法融合，模型蒸馏，缺陷特征检索，Stable diffusion的Inpaint应用，AnyDoor项目开发，Cuda算子的替换以及大模型语言技术引入到公司内的标注软件DLTools和算法检测平台软件TImesAI。小组组员最多的时候有三位，深度学习小组负责公司软件深度学习模块依赖升级和部署，标注软件的立项开发和维护，项目定制算法，深度学习算法升级和维护，大模型算法应用。深度学习相关部署框架有接触过ncnn, tensorrt, onnx, 创建过视觉大模型训练流程，用过python和cpp的进程间通信，socket和共享内存。



在平台算法软件上，因为这是一家创业不久的公司，平台算法软件在去年才有稳定版，过往软件维护非常依赖群内甚至是语音沟通，我们希望能引入垂直方向的大模型应用帮助我们解决人工维护成本过高的问题。我们先后引入简单RAG，发现检索成功率不高，容易出现版本信息混淆，没法处理多跳问题和缺失信息的问题。我后续引入GraphRAG和LangGraph解决这些问题，整个工作流程大致分为意图识别，安全护栏，子任务分解-幻觉拦截，整个Agent拥有调用多种工具的能力，整体回答效率控制在业务接受范围内。意图识别最早是promtp加上qwen大模型或者deepseek大模型，我在prompt里加入三类标签--general, additional, graphrag。后续发现意图种类基本不变后，改用embedding模型进行意图识别，同时加入一些负样本进行优化，意图识别F1提高到0.89，相对提高了接近10%。普通rag的检索资料来源有三块，对话生成，资料总结，文档补充。embedding数据集里有query类似的文档，也有文档的chunk

在DLTools里，先后引入简单的RAG，负责简单的SQL生成；引入了NL2SQL的SQL Agent，负责复杂的SQL生成，在此基础上，我微调了一下qwen0.6b，构建灵活的query问题生成流程，修改prompt让云端deepseek模型产生更好的合成数据，经过sft微调后，qwen3 0.6b模型能在langchain的sql agent里以更少的chain轮次解决问题。引入一个意图识别功能，区分简单RAG，复杂NL2SQL，模型对比和数据匹配。其中模型对比和数据匹配是软件原有的功能，我将他们制作成模型可以调用的MCP。

